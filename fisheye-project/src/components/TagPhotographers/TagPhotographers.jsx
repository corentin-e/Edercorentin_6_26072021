import data from './data'

const TagPhotographer = () => (
    <div>
        {data.photographers.map(photographer => (
            
            ))
        }
    </div> 
    
)
export default TagPhotographer;